Sean Clayton

To compile program:
gcc smallsh.c -g -o smallsh

To run program:
./smallsh

To compile test script:
chmod +x ./p3testscript

To run test script:
1. First compile and run the shell (steps above)
2. Within the shell, type:
    ./p3testscript-1 2>&1